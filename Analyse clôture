def run():
    # Remplace ce contenu par ton script existant
    print("Lancement de l’analyse automatique…")
    # 👇 Ton code ici (lecture Excel, calculs, etc.)
    print("Analyse terminée avec succès.")
from flask import Flask
import analyse_cloture

app = Flask(__name__)

@app.route('/', methods=['GET', 'POST'])
def lancer():
    analyse_cloture.run()
    return "✅ Analyse exécutée."

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=10000)
flask
services:
  - type: web
    name: analyse-cloture
    env: python
    buildCommand: ""
    startCommand: "python app.py"
